{
  "name": "fonekist",
  "version": "0.1.0",
  "private": true,
  "description": "FONEKIST \u2014 phone-only Pakistani storefront on the shared Medusa commerce backend.",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node scripts/derive-media.mjs && next dev --port 3001",
    "build": "node scripts/derive-media.mjs && next build && node scripts/prepare-standalone.mjs",
    "start": "next start --port 3001",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:a11y": "playwright test tests/e2e/accessibility.spec.ts",
    "sync:contracts": "node scripts/sync-contracts.mjs",
    "sync:media": "node scripts/sync-media.mjs && node scripts/derive-media.mjs",
    "loadtest": "node scripts/loadtest.mjs",
    "serve": "node scripts/serve.mjs",
    "derive:media": "node scripts/derive-media.mjs"
  },
  "dependencies": {
    "next": "16.3.2",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "sharp": "^0.35.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.49.1",
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.1",
    "autocannon": "^8.0.0",
    "eslint": "^9.17.0",
    "eslint-config-next": "16.3.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
